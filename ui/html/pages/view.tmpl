{{define "title"}}Phrase{{end}}

{{define "main"}}
    {{if .Phrases}}
    {{range $index, $value := .Phrases}}
    <div id="phrase{{$index}}" class="phraseTable" >
        <table>
            <tr>
                <td>{{.Phrase}}</td>
            </tr>
         </table>
         <table>
            <tr>
                <td><input id="solution{{$index}}" type="text" placeholder="solución"></td>
            </tr>
         </table>
         <table>
            <tr>
               <td id="answer{{$index}}" class="answer">{{.Translates}}</td>
             </tr>
         </table>
    </div>
    {{end}}
    {{end}}
    <div id="tableButtons" class="tableButtons">
        <table>
            <tr>
                <td><button id="hintButton">Hint</button></td>
                <td><button id="showButton">Show</button></td>
                <td><button id="acceptButton">Accept</button></td>
                <td><button id="rejectButton">Reject</button></td>
            </tr>
        </table>
    </div>
    <table>
        <tr>
            <td><button id="startButton">Start</button></td>
        </tr>
    </table>
    <br>
    {{if .Phrases}}
     <table>
        <tr>
            <th>Phrase</th>
            <th>Translates</th>
            <th>ID</th>
         </tr>
        {{range .Phrases}}
        <tr>
            <!-- Use the new clean URL style-->
            <td>{{.Phrase}}</td>
            <td>{{.Translates}}</td>
            <td>#{{.ID}}</td>
        </tr>
        {{end}}
    </table>
    <script src="/static/js/view.js" type="text/javascript"></script>
    {{else}}
        <p>There's nothing to see here... yet!</p>
    {{end}}
{{end}}